# Keep unit tests hermetic: the merged manifest registers ScreenPalApplication,
# whose onCreate() now launches a real Piper warm-up (model download over network).
# Default all sandboxes to a plain Application; ScreenPalApplicationWarmUpTtsTest
# overrides this per-class via @Config(application=...) where needed.
application=android.app.Application
